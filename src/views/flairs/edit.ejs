<!DOCTYPE html>
<html>

<head>
    <% include ../static/partials/head.ejs %>
</head>

<body>
    <% include ../static/partials/navbar.ejs %>

    <main class="container">

        <h1>Edit Flair</h1>

        <form action="/topics/<%= flair.topicId %>/flairs/<%= flair.id %>/update" method="post">
            <div class="form-group">
                <label for="name">Tag name</label>
                <input value="<%= flair.name %>" type="text" class="form-control" name="name"
                    aria-describedby="titleHelp" placeholder="Enter tag name">
            </div>
            <div class="form-group">
                <label for="color">Tag color</label>
                <textarea rows="1" class="form-control" name="color"
                    placeholder="Enter a valid html color"><%= flair.color %></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Update</button>
        </form>
    </main>

    <% include ../static/partials/baseScripts.ejs %>

</body>

</html>